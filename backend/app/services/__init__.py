"""Service layer for the Memory Rhythm demo.

Contains the orchestrator state machine and the individual cognitive services
(reasoner / dialogue / memory / autobiography / community). These modules are
authored by the services agent; this package marker keeps ``app.services`` a
proper importable package.
"""
