# Runtime deps for deploying the Memory Rhythm backend (single-host, WS-capable).
# Lean set: core API/WS + Google Gemini (LLM/Image/Embedding). STT/TTS stay mock
# (no heavy google-cloud-speech / openai), so a plain `pip install -r` is fast.
# Mirrors backend/pyproject.toml [project.dependencies].
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
websockets>=12.0
python-multipart>=0.0.9
httpx>=0.27
numpy>=1.26
google-genai>=0.3
boto3>=1.34   # Amazon Polly TTS (TTS_PROVIDER=polly)
